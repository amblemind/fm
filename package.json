{
  "name": "madeby-tag",
  "private": true,
  "version": "1.0.0",
  "description": "Design a \"Made by\" tag for your website and copy the code. No build step, no dependencies, no account.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.madeby.config.js",
    "preview": "vite preview"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.56.10",
    "vite": "^8.2.2"
  }
}
